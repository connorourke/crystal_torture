# Core dependencies
numpy>=1.19.0
pymatgen>=2022.0.0

# Development and testing dependencies
pytest>=6.0
pytest-cov
coverage
ddt

# Note: f90wrap is installed as a build dependency via pyproject.toml
